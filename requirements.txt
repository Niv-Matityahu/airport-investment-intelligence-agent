# Airport Investment Intelligence Agent
# LLM providers (either works; pick via LLM_PROVIDER / which key is set)
anthropic>=0.69
google-genai>=1.0
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
requests>=2.28
openpyxl>=3.1
streamlit>=1.38          # needs st.audio_input (voice) + st.dialog (full-screen)
pydeck>=0.8              # the opportunity map
markdown>=3.0
